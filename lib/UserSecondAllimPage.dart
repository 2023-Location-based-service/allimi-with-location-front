import 'package:flutter/material.dart';

class UserSecondAllimPage extends StatelessWidget {
  const UserSecondAllimPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('개인알림장'),
      ),
    );
  }
}

